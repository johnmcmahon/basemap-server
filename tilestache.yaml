layers:
    grey:
        projection: WGS84
        provider:
            name: mapnik
            mapfile: /server/themes/grey.xml
    sepia:
        projection: WGS84
        provider:
            name: mapnik
            mapfile: /server/themes/sepia.xml
    silver:
        projection: WGS84
        provider:
            name: mapnik
            mapfile: /server/themes/silver.xml


cache:
    name: Disk
    path: /server/cache
